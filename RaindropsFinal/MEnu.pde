class Menu {
  PVector location;
  float boxHeight = 30;
  float boxWidth = 100;

  class Box extends Menu{
      
  }
//  Menu(int _boxes, int x, int y, String a, String b) {
//
//    boxes = new Box [_boxes];
//
//    for (int i = boxes.length-1; i >= 0; i--) {
//        for (float j = boxHeight * 1.5; j <= _boxes * boxHeight; j++) {
//        boxes[i] = new Box(width/2, j, boxWidth, boxHeight, "");
//      }
//    }
//    boxes[0].dialogue = a;
//    boxes[1].dialogue = b;
//  }
//
//  void display() {
//    for (int i = boxes.length - 1; i >= 0; i--) {
//      boxes[i].display();
//    }
//  }
}

